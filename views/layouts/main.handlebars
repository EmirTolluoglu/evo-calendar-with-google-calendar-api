<!DOCTYPE html>
<html lang="en">

<head>
    <title>Evo Calendar</title>
    <meta charset="utf-8">
    <meta name="description" content="Simple Modern-looking Event Calendar Plugin">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <link rel="stylesheet" type="text/css" href="/css/evo-calendar.min.css">
</head>

<body>
    <div id="evoCalendar"></div>
<script src="/js/jquery.min.js"></script>
<script src="/js/evo-calendar.min.js"></script>
<script>

    $(document).ready(function() {
        const eventTypes = Array('event', 'holiday', 'birthday');
    $("#evoCalendar").evoCalendar({
        format: "MM dd, yyyy",
        titleFormat: "MM",
        language: "tr",
        todayHighlight: true,
        sidebarToggler: true,
        eventHeaderFormat: "MM dd, yyyy",
        firstDayOfWeek: 1,
        calendarEvents: [{{#each eventList}}{
            
            id: "{{@index}}",
            name: "{{this.name}}",
            date: "{{this.date}}",
            type: eventTypes[Math.floor(Math.random() * eventTypes.length)],
            
        },{{/each}}]
    });


});
</script>
</body>

</html>